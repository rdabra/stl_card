\documentclass[10pt]{article}
\usepackage[a4paper,margin=5mm,landscape]{geometry}

\usepackage{multicol}
\usepackage{enumitem}
\usepackage[dvipsnames]{xcolor}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{float}

\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{amsthm}

% ---------- typography ----------
\usepackage[T1]{fontenc}
\usepackage[sfdefault,condensed]{roboto}
\usepackage[scaled=0.84]{inconsolata}
\usepackage{microtype}

\renewcommand{\familydefault}{\sfdefault}
\linespread{0.98}
\setlength{\parindent}{0pt}
\pagestyle{empty}

% ---------- colors ----------
\definecolor{PageBg}{RGB}{250,250,247}
\definecolor{AccentBlue}{RGB}{28,84,140}
\definecolor{RuleGray}{RGB}{180,186,195}
\definecolor{BodyText}{RGB}{34,38,42}
\definecolor{CodeBg}{RGB}{242,244,247}
\definecolor{CommentGreen}{RGB}{64,120,90}
\definecolor{StringRed}{RGB}{160,68,72}
\definecolor{PreprocPurple}{RGB}{122,79,153}

\pagecolor{PageBg}
\color{BodyText}
\colorlet{Blue}{BodyText}

% ---------- layout ----------
\setlength{\columnsep}{7pt}
\setlength{\columnseprule}{0.25pt}
\setlength{\textfloatsep}{2pt}
\setlength{\floatsep}{2pt}
\setlength{\intextsep}{2pt}

% ---------- links ----------
\hypersetup{
  colorlinks=true,
  urlcolor=AccentBlue,
  linkcolor=AccentBlue,
  citecolor=AccentBlue
}
\urlstyle{same}

% ---------- section titles ----------
\usepackage{titlesec}
\titleformat{\subsection}
  {\color{AccentBlue}\large\bfseries\scshape}
  {}{0pt}{}
  [{}]

\titleformat{\subsubsection}
  {\color{AccentBlue}\normalsize\bfseries\scshape}
  {}{0pt}{}

\titlespacing*{\subsection}{0pt}{4pt}{2pt}
%\titlespacing*{\subsubsection}{0pt}{2.5pt}{1pt}

\titlespacing*{\subsubsection}{0pt}{0.5pt}{0.5pt}
%\setlist[itemize]{leftmargin=*,topsep=0pt,partopsep=0pt,parsep=0pt,itemsep=-1.6pt}

% ---------- code ----------
\usepackage{listings}
\lstdefinestyle{mystyle}{
  language=C++,
  aboveskip=2pt,
  belowskip=2pt,
  basicstyle=\fontsize{6.2}{7.0}\ttfamily\color{BodyText},
  keywordstyle=\color{AccentBlue}\bfseries,
  stringstyle=\color{StringRed},
  commentstyle=\color{CommentGreen}\itshape,
  morecomment=[l][\color{PreprocPurple}]{\#},
  backgroundcolor=\color{CodeBg},
  frame=single,
  framerule=0.25pt,
  rulecolor=\color{RuleGray},
  breaklines=true,
  breakatwhitespace=false,
  showstringspaces=false,
  columns=fullflexible,
  keepspaces=true,
  tabsize=2,
  xleftmargin=2pt,
  xrightmargin=2pt
}

% ---------- helper ----------
\newcommand{\softseparator}{%
  \par\noindent{\color{RuleGray}\rule{\linewidth}{0.35pt}}\vspace{1pt}
}

\begin{document}
\noindent
{\fontsize{15}{16}\selectfont\bfseries\textit{C++ Standard Library Cheat Sheet}}
\hfill{\footnotesize\color{AccentBlue}version 1.5}

{\footnotesize Roberto D. Algarte (2026)}
\softseparator

\scriptsize
\begin{multicols*}{4}

{\color{Blue}
\subsection*{\center{\textsc{notation}}}	

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{priority\_queue} @ $<$queue$>$: class ``std::pri\-ori\-ty\_que\-ue'' that belongs to library ``queue''. 
	\item \emph{\textbf{setbase}} @ $<$iomanip$>$: standalone function ``std::setbase'' that belongs to library ``iomanip'';
	\item basic\_ios::\emph{\textbf{good}} @ $<$iostream$>$: member function ``good'' of an object from class ``std::basic\_ios'' that belongs to library ``iostream''; 
	\item Items in lists are alphabetically sorted; 
	\item Section titles are linked to \href{https://en.cppreference.com}{\underline{cppreference.com}} 
\end{itemize}

\softseparator

}

{\color{Blue}
\subsection*{\href{https://en.cppreference.com/w/cpp/container}{\underline{CONTAINERS}}}	
\noindent
Aggregates of elements that provide element insertion, removal and access.

\subsubsection*{\textsc{Theory} - \emph{Time Complexity}} 
\begin{figure}[H]
\begin{center}
\IfFileExists{complexity.jpg}{\includegraphics[scale=.21]{complexity.jpg}}{\fbox{\parbox[c][3.2cm][c]{0.88\linewidth}{\centering Complexity image\\(complexity.jpg)}}}	
\end{center}
\end{figure}

\subsubsection*{\textsc{Theory} - \emph{Linear Aggregates}} 

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \emph{Array}: static or dynamic structure whose elements have defined positions and are stored in contiguous memory. Insertion and removal of elements may imply repositioning of other elements or repositioning of the entire structure; 
	\item \emph{Linked list}: dynamic structure whose elements may not be stored contiguously. A node (list element) is composed by a value and one or two references to adjacent nodes. Nodes of a \emph{doubly-linked} list have two references, and of a \emph{singly-linked}, one reference. Insertion and removal don't imply repositioning of other elements;
	\item \emph{Queue}: FIFO dynamic structure with two ends, one for insertion and another for removal; access can be performed on both. In a \emph{double-ended} queue, both ends allow insertion and removal;  
	\item \emph{Stack}: LIFO dynamic structure with one end, in which insertion, deletion and access are performed;
\end{itemize}

\subsubsection*{\textsc{Sequence Containers}} 
\noindent
Containers whose elements can be accessed sequentially and are stored contiguously on the heap memory, except the array.

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{array} @ $<$array$>$: wrapper for C static arrays. The elements are created on the stack by default;
	\item \textbf{deque} @ $<$deque$>$: implementation of a double-ended queue. The elements are created on the heap by default. Insertion, removal and access are $O(1)$;
	\item \textbf{forward\_list} @ $<$forward\_list$>$: implementation of a singly-linked list. The elements are created on the heap by default. Insertion and removal are $O(1)$; access is $O(n)$. Provided by $<$forward\_list$>$;  
	\item \textbf{list} @ $<$list$>$: implementation of a doubly-linked list. The elements are created on the heap by default. Insertion and removal are $O(1)$; access is $O(n)$;
  \item \textbf{span} @ $<$span$>$: a non-owing view (reference) of contiguous sequence containers such as C array, \textbf{array}, \textbf{vector}, \textbf{string}. It can  reference these containers indiscriminately;
	\item \textbf{vector} @ $<$vector$>$: implementation of a dynamic array. The elements are created on the heap by default. Insertion and removal are $O(1)$ at the end, $O(n)$ elsewhere; access is $O(1)$.
\end{itemize}

\subsubsection*{\textsc{Sequence Container Adapters}} 
\noindent
Wrappers created around \textbf{deque} to provide other types of aggregates.

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{priority\_queue} @ $<$queue$>$: implementation of a queue in which the front element must observe a priority condition (the largest, by default). Insertion and removal are $O(log(n))$;
	\item \textbf{queue} @ $<$queue$>$: implementation of a simple queue;
	\item \textbf{stack} @ $<$stack$>$: implementation of a stack;
\end{itemize}
\noindent

\subsubsection*{\textsc{Associative Containers}} 
\noindent
Containers constituted by (non)primary key sorted elements or pair of (non)primary key-value sorted elements that are  inserted, removed and accessed in $O(log(n))$ time complexity for the worst case.

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{map} @ $<$map$>$: constituted by primary key-value elements;
	\item \textbf{multimap} @ $<$map$>$: constituted by non primary key-value elements;  
	\item \textbf{multiset} @ $<$set$>$: constituted by non primary key elements;
	\item \textbf{set} @ $<$set$>$: constituted by primary key elements;
\end{itemize}
\noindent
Each one of these types has its \textbf{unordered\_*} counterpart, whose elements are inserted or removed in $O(1)$, and accessed in $O(n)$ for the worst case. 

\subsubsection*{\textsc{Main Member Functions}  @container$>$} 
\noindent Some functions are not valid to all containers.

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item container::\emph{\textbf{clear}}: empties the container;
	\item container::\emph{\textbf{emplace\_back}}: performs a container::\emph{\textbf{emplace}} at the end iterator;
	\item container::\emph{\textbf{emplace\_front}}: performs a container::\emph{\textbf{emplace}} at the begin iterator;
	\item container::\emph{\textbf{emplace}}: creates and inserts an object before a specified iterator and returns the iterator of this inserted object;
	\item container::\emph{\textbf{erase}}: deletes elements in a specified range;
	\item container::\emph{\textbf{insert}}: inserts a copy of an object before a specified iterator and returns the iterator of this inserted object;
	\item container::\emph{\textbf{push\_back}}: performs a container::\emph{\textbf{insert}} at the end iterator;
	\item container::\emph{\textbf{push\_front}}: performs a container::\emph{\textbf{insert}} at the begin iterator;
\end{itemize}
\noindent

\subsubsection*{\textsc{Practical Member Functions}  @$<$container$>$} 
\noindent These are the ones you end up reaching for constantly.

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
  \item container::\emph{\textbf{size}}: number of elements currently stored;
  \item container::\emph{\textbf{empty}}: checks if \emph{size == 0};
  \item container::\emph{\textbf{begin/end}}: iterators to the first element / one-past-the-last element;
  \item container::\emph{\textbf{cbegin/cend}}: const iterators (read-only);
  \item container::\emph{\textbf{front/back}}: references to first/last element (\textbf{precondition:} not empty);
  \item container::\emph{\textbf{swap}}: swaps contents in $O(1)$ (typically by swapping pointers/metadata);
\end{itemize}

\subsubsection*{\textsc{Vector functions} @ $<$vector$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
  \item vector::\emph{\textbf{operator[]}}: unchecked random access;
  \item vector::\emph{\textbf{at}}: checked access (throws \textbf{std::out\_of\_range});
  \item vector::\emph{\textbf{data}}: pointer to contiguous storage (\emph{C array view});
  \item vector::\emph{\textbf{capacity}}: number of elements that fit before reallocation;
  \item vector::\emph{\textbf{reserve}}: grows capacity (does \textbf{not} change size);
  \item vector::\emph{\textbf{resize}}: changes size (creates/destroys elements);
  \item vector::\emph{\textbf{shrink\_to\_fit}}: non-binding request to release unused capacity;
  \item \emph{\textbf{std::erase / std::erase\_if}}: convenient erase for \textbf{vector}. For conditional erase, preserves the elements that obeys the predicate;
\end{itemize}

\subsubsection*{\textsc{Deque functions} @ $<$deque$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
  \item deque::\emph{\textbf{push\_front/push\_back}}: inserts at both ends in $O(1)$;
  \item deque::\emph{\textbf{emplace\_front/emplace\_back}}: constructs at both ends in-place;
  \item deque::\emph{\textbf{pop\_front/pop\_back}}: removes from both ends in $O(1)$;
  \item deque::\emph{\textbf{operator[]/at}}: random access exists (but storage is \textbf{not contiguous});
  \item \emph{\textbf{std::erase / std::erase\_if}}: convenient erase for \textbf{deque}. For conditional erase, preserves the elements that obeys the predicate;
\end{itemize}

\subsubsection*{\textsc{List functions} @ $<$list$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
  \item list::\emph{\textbf{push\_front/push\_back}}: inserts a copy at the beginning/end in $O(1)$;
  \item list::\emph{\textbf{pop\_front/pop\_back}}: removes the first/last element in $O(1)$;
  \item list::\emph{\textbf{insert}}: inserts a copy before a specified iterator and returns the iterator to the inserted element;
  \item list::\emph{\textbf{remove}}: removes all elements equal to a specified value;
  \item list::\emph{\textbf{remove\_if}}: removes all elements that obey a predicate;
  \item list::\emph{\textbf{unique}}: removes consecutive duplicated elements;
  \item list::\emph{\textbf{sort}}: sorts the elements using the list's member sort;
  \item list::\emph{\textbf{merge}}: merges another sorted list into the current sorted list;
  \item list::\emph{\textbf{reverse}}: reverses the order of the elements;
  \item list::\emph{\textbf{splice}}: transfers nodes from another \textbf{list} to a specified position without copying elements;
  \item list::\emph{\textbf{front/back}}: references to the first/last element (\textbf{precondition:} not empty);
\end{itemize}

\subsubsection*{\textsc{unordered\_map functions} @ $<$unordered\_map$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
  \item unordered\_map::\emph{\textbf{at}}: access without insertion (throws if missing);
  \item unordered\_map::\emph{\textbf{bucket\_count}}: number of buckets (changes on rehash);
  \item unordered\_map::\emph{\textbf{contains}}: membership test (no iterator);
  \item unordered\_map::\emph{\textbf{extract}}: extracts a row from the map and returns a node handle object;
  \item unordered\_map::\emph{\textbf{merge}}: extracts different-key rows from another map and includes in this;
  \item unordered\_map::\emph{\textbf{find}}: returns iterator to key or \emph{end()};
  \item unordered\_map::\emph{\textbf{insert\_or\_assign}}: insert if missing, otherwise overwrite value;
  \item unordered\_map::\emph{\textbf{load\_factor/max\_load\_factor}}: hash-table density controls;
  \item unordered\_map::\emph{\textbf{operator[]}}: access \textbf{with insertion} of default value if key missing;
  \item unordered\_map::\emph{\textbf{reserve/rehash}}: control bucket growth and reduce rehash events;
  \item unordered\_map::\emph{\textbf{try\_emplace}}: inserts only if missing; constructs value only on insertion;
\end{itemize}

\subsubsection*{\textsc{Iterator invalidation (quick rules)}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
  \item \textbf{deque}: safest assumption is \emph{mutating operations can invalidate iterators}; do not keep iterators across push/pop/insert/erase unless you check the exact rule;
  \item \textbf{unordered\_map}: \emph{rehash} invalidates \textbf{all iterators}; references/pointers to elements generally remain valid, but iterators do not;
  \item \textbf{vector}: reallocation invalidates \emph{all} iterators/references/pointers; erase/insert invalidates from the point of change to the end;
\end{itemize}

\subsubsection*{\textsc{Main Member Types}  @$<$container$>$} 
\noindent These types are present only in sequence containers.

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item container::\textbf{const\_iterator}: declares a const iterator;
	\item container::\textbf{const\_reverse\_iterator}: declares a const reverse iterator;
	\item container::\textbf{iterator}: declares an iterator;
	\item container::\textbf{reverse\_iterator}: declares as reverse iterator;
\end{itemize}

}

\softseparator

{\color{Blue}
\subsection*{\href{https://en.cppreference.com/w/cpp/io}{\underline{I/O STREAMS}}}	
\noindent
Streams enable flow of data to be written (output) or to be read (input). Output streams can be buffered or not.
The \emph{extractor operator} $<<$ when applied to input streams performs formatted input while the \emph{insertion operator} $>>$ applied to output streams performs formatted output. 

\subsubsection*{\textsc{Console Streams}  @$<$iostream$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{cerr}: wrapper around the standard C stream stderr, which writes data to the console;
	\item \textbf{cin}: wrapper around the standard C stream stdin, which reads data from the console;
	\item \textbf{cout}: wrapper around the standard C stream stdout, which writes data to the console and is buffered;
\end{itemize}

\subsubsection*{\textsc{String Streams}  @$<$sstream$>$}
\noindent
Streams that enable flow of outputted or inputted strings.

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{basic\_istringstream}: provides high level string input formatting;
	\item \textbf{basic\_ostringstream}: provides high level string output formatting;
	\item \textbf{basic\_stringstream}: provides high level string input/output formatting;
\end{itemize}

\subsubsection*{\textsc{File Streams} @$<$fstream$>$} 
\noindent
Streams that enable flow of outputted or inputted data to or from files.

\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{basic\_ifstream}: provides high level file input formatting;
	\item \textbf{basic\_fstream}: provides high level file input/output formatting;
	\item \textbf{basic\_ofstream}: provides high level file output formatting;
\end{itemize}

\subsubsection*{\textsc{Aliases}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
  \item \textbf{ifstream} $=$ \textbf{basic\_ifstream}<char>;
  \item \textbf{istringstream} $=$ \textbf{basic\_istringstream}<char>;
  \item \textbf{fstream} $=$ \textbf{basic\_fstream}<char>;
  \item \textbf{ofstream} $=$ \textbf{basic\_ofstream}<char>;
  \item \textbf{ostringstream} $=$ \textbf{ba\-sic\_o\-string\-stream}\-<char>;
  \item \textbf{stringstream} $=$ \textbf{basic\_stringstream}<char>;
\end{itemize}


\subsubsection*{\textsc{Manipulators and Formatters}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{boolalpha} @ $<$ios$>$: sets text values for booleans;
	\item \textbf{defaultfloat} @ $<$ios$>$: outputs floats as default;
	\item \emph{\textbf{endl}} @ $<$ostream$>$: inserts a line break and flushes the buffer; 
	\item \emph{\textbf{ends}} @ $<$iomanip$>$: inserts a null character; 
	\item \textbf{fixed} @ $<$ios$>$: outputs floats in fixed width;
	\item \emph{\textbf{flush}} @ $<$ostream$>$: flushes the stream buffer; 
	\item \emph{\textbf{get\_money}} @ $<$iomanip$>$: parse value as currency; 
	\item \emph{\textbf{get\_time}} @ $<$iomanip$>$: parses value as a datetime; 
	\item \emph{\textbf{getline}} @ $<$string$>$:  reads a line from input stream setting the reading pointer to the new line. By default, lines are separated by line breaks;
	\item \textbf{hexfloat} @ $<$ios$>$: outputs floats in hex notation;
	\item \textbf{hex} @ $<$ios$>$: outputs in base 16;
	\item \textbf{left} @ $<$ios$>$: sets left as the output position of the fill char;
	\item \textbf{oct} @ $<$ios$>$: outputs in base 8;
	\item \emph{\textbf{put\_money}} @ $<$iomanip$>$: sets values to currency; 
	\item \emph{\textbf{put\_time}} @ $<$iomanip$>$: outputs a \textbf{std:tm} object with specified format; 
	\item \emph{\textbf{quoted}} @ $<$iomanip$>$: correctly reads and writes strings with quotes; 
	\item \textbf{right} @ $<$ios$>$: sets right as the output position of the fill char;
	\item \textbf{scientific} @ $<$ios$>$: outputs floats in scientific notation;
	\item \emph{\textbf{setbase}} @ $<$iomanip$>$: sets the output base; 
	\item \emph{\textbf{setfill}} @ $<$iomanip$>$:sets a fill character instead of white space; 
	\item \emph{\textbf{setprecision}} @ $<$iomanip$>$: sets the precision of floats; 
	\item \emph{\textbf{setw}} @ $<$iomanip$>$: sets the output width for the text; 
	\item \textbf{showbase} @ $<$ios$>$: enables base prefix in numbers;
	\item \textbf{showpoint} @ $<$ios$>$: enables decimal point in numbers;
	\item \textbf{showpos} @ $<$ios$>$: pre-print a $+$ in nonnegative numbers;
	\item \textbf{skipws} @ $<$ios$>$: skips white spaces;
	\item \textbf{unitbuf} @ $<$ios$>$: sets buffer flushing for every output operation;
	\item \textbf{uppercase} @ $<$ios$>$: sets the output chars to be uppercased;
\end{itemize}


\subsubsection*{\textsc{States}  @$<$iostream$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item basic\_ios::\emph{\textbf{bad}}: stream is in a non recoverable error state;
	\item basic\_ios::\emph{\textbf{clear}}: refresh the state of the stream;
	\item basic\_ios::\emph{\textbf{eof}}: reading pointer arrived at the end of file;
	\item basic\_ios::\emph{\textbf{fail}}: stream is in a recoverable error state;
	\item basic\_ios::\emph{\textbf{good}}: stream is fine an can be read or written to;
\end{itemize}

}

\softseparator

{\color{Blue}
\subsection*{\href{https://en.cppreference.com/w/cpp/iterator}{\underline{ITERATORS}}}	
\noindent
Pointer-like types created to implement the iterator pattern in STL. The iterator pattern states that aggregate structures and element-accessing functions should be decoupled.

\subsubsection*{\textsc{Iteratable Aggregates}} 
\noindent
All types of STL sequence containers, C arrays, input and output streams.

\subsubsection*{\textsc{Concepts} @$<$iterator$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{bidirectional\_iterator}: a \textbf{for\-ward\_ite\-ra\-tor} able to traverse backwards; 
	\item \textbf{contiguous\_iterator}: a specific \textbf{ran\-dom\_ac\-cess\_i\-te\-ra\-tor} that traverses elements stored contiguously in memory (e.g. vector). 
	\item \textbf{forward\_iterator}: an \textbf{input\_iterator} that has equality comparison and multi-pass, which means that it can traverse multiple times the elements of an aggregate (e.g. containers); 
	\item \textbf{input\_iterator}: a type that can be both pre- and post-incremented, related to a given structures whose values can be read (e.g. input streams);
	\item \textbf{istream\_iterator}: a specific type of \textbf{in\-put\_i\-te\-ra\-tor} that reads from an input stream;
	\item \textbf{ostream\_iterator}: a specific type of \textbf{out\-put\_i\-te\-ra\-tor} that writes to an output stream;
	\item \textbf{output\_iterator}: a type that can be both pre- and post-incremented, related to a given structure to which values can be written (e.g. output streams);
	\item \textbf{random\_access\_iterator}: a \textbf{bidirectional\_iterator} that supports subscripting with $O(1)$ time access to elements; 
\end{itemize}

\subsubsection*{\textsc{Range Functions}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{begin}} @ $<$iterator$>$: returns the first element read-write iterator. The function \emph{\textbf{cbegin}} returns the read-only version;
	\item  \emph{\textbf{end}} @ $<$iterator$>$: returns the one-past-the-last element read-write iterator. The function \emph{\textbf{cend}} returns the read-only version;
	\item  \emph{\textbf{rbegin}} @ $<$iterator$>$: returns the first element read-write reverse iterator. The function \emph{\textbf{crbegin}} returns the read-only version;
 	\item  \emph{\textbf{rend}} @ $<$iterator$>$: returns the one-past-the-last element read-write reverse iterator. The function \emph{\textbf{crend}} returns the read-only version;
	\item  \emph{\textbf{ranges::remove\_if}} @ $<$algorithm$>$: moves all the elements of a range that obey a predicate to the end of the container and returns the removed range; 
	\item  \emph{\textbf{ranges::remove}} @ $<$algorithm$>$: moves all the elements of a range equal to a specified value to the end of the container and returns the moved range;
	\item  \emph{\textbf{ranges::reverse\_view}} @ $<$ranges$>$: returns the reversed range of its argument;

\end{itemize}

\subsubsection*{\textsc{Adapters} @$<$iterator$>$} 
\noindent
Iterator wrappers created to encapsulate some important iterator-related functionalities. 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{back\_insert\_iterator}: performs back insertion of elements. This adapter can be created by the helper function \emph{\textbf{back\_inserter}};
	\item \textbf{front\_insert\_iterator}: performs front insertion of elements. This adapter can be created by the helper function \emph{\textbf{front\_inserter}};
	\item \textbf{insert\_iterator}: performs insertion of element at a specified position. This adapter can be created by the helper function \emph{\textbf{inserter}};
	\item \textbf{move\_iterator}: performs move of elements from one aggregate to another. This adapter can be created by the helper function \emph{\textbf{make\_move\_iterator}};
	\item \textbf{reverse\_iterator}: performs reverse order traversal. This adapter can be created by the helper function \emph{\textbf{make\_reverse\_iterator}};
\end{itemize}

\subsubsection*{\textsc{Operations} @$<$iterator$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{advance}}: advances an iterator by a given distance;
	\item  \emph{\textbf{data}}: returns the pointer to the given sequence container;
	\item  \emph{\textbf{distance}}: returns the number of elements between two iterators in which the first is included and last excluded;
	\item  \emph{\textbf{empty}}: checks whether the given range empty;
	\item  \emph{\textbf{next}}: returns the next iterator;
	\item  \emph{\textbf{prev}}: returns the previous iterator;
\end{itemize}
}

\softseparator

{\color{Blue}
\subsection*{\href{https://en.cppreference.com/w/cpp/algorithm}{\underline{ALGORITHMS}}}	
\noindent  
Collection of libraries that handle ranges of elements providing a variety of functionalities like searching, counting, sorting, partitioning and transforming.

\subsubsection*{\textsc{Numeric Functions}  @$<$numeric$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{accumulate}}: sums up or folds sequentially a range of elements;
		\item  \emph{\textbf{adjacent\_difference}}: computes the difference between two adjacent values in a range storing them in the original range;
	\item  \emph{\textbf{inner\_product}}: returns the inner product of two ranges;
	\item  \emph{\textbf{iota}}: fills a range with successive increments of the starting value;
	\item  \emph{\textbf{inclusive\_scan}}: sums up or folds the elements from beginning to each element position, storing each result in the original range, according to the execution policy;
	\item  \emph{\textbf{partial\_sum}}: sums up or folds sequentially the elements from beginning to each element position, storing each result in the original range;
\item  \emph{\textbf{reduce}}: sums up or folds a range of elements according to the execution policy (it may reorder the elements);
	\item  \emph{\textbf{transform\_reduce}}: re-implementation of \emph{\textbf{inner\_product}} with execution policy support;
	\end{itemize}

\subsubsection*{\textsc{Sorting Functions} @$<$algorithm$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{is\_sorted\_until}}: finds the largest sub-range inside a range whose elements are non-descending ordered;
	\item  \emph{\textbf{is\_sorted}}: checks if the elements are non-descending ordered;
	\item  \emph{\textbf{nth\_element}}: rearranges the elements in such a way that the value at the position n corresponds to the n-th element of the sorted range;
	\item  \emph{\textbf{partial\_sort\_copy}}: copies an ascending sorts from begin until a specified position of a range to another range;
	\item  \emph{\textbf{partial\_sort}}: sorts a range in ascending order from begin until a specified position;
	\item  \emph{\textbf{sort}}: sorts a range in ascending order;
	\item  \emph{\textbf{stable\_sort}}: sorts a range in ascending order such that the original order of equally indexed elements of multi-associative containers is preserved;
\end{itemize}

\subsubsection*{\textsc{Main Modifying Functions} @$<$algorithm$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{copy\_backward}}: copies a range, from end to beginning, to another range;
	\item  \emph{\textbf{copy\_if}}: copies a range, from beginning to end, to another range that obey a predicate;
	\item  \emph{\textbf{copy\_n}}: copies n elements of a range, from beginning to end, to another range;
	\item  \emph{\textbf{copy}}: copies a range, from beginning to end, to another range;
	\item  \emph{\textbf{fill}}: copies a specified value to all elements in a range;
	\item  \emph{\textbf{generate}}: populates a range through a callable object;
	\item  \emph{\textbf{is\_permutation}}: checks whether a range is a permutation of another range;
	\item  \emph{\textbf{iter\_swap}}: swaps values pointed by two iterators;
	\item  \emph{\textbf{move\_backward}}: moves the elements of a range, from end to beginning, to another range. Afterwards, the elements of the source range are left in undefined state;
	\item  \emph{\textbf{move}}: moves the elements of a range, from beginning to end, to another range. Afterwards, the elements of the source range are left in undefined state;
	\item  \emph{\textbf{next\_permutation}}: performs the next permutation in a range returning false if it's the last permutation. The reference is the sorted range;
	\item  \emph{\textbf{partition\_copy}}: copies the elements of a range to two other ranges so that one target range receives elements that obey a predicate and the other receives the remaining elements;
	\item  \emph{\textbf{partition}}: reorders the elements of a range so that elements that obey a predicate precede the elements that don't. The relative order of the elements may not be preserved;
	\item  \emph{\textbf{prev\_permutation}}: performs the previous permutation in a range returning true if it's the first permutation. The reference is the sorted range;
	\item  \emph{\textbf{remove\_copy\_if}}: copies all the elements of a range to another that disobey a predicate; 
	\item  \emph{\textbf{remove\_copy}}: copies all the elements of a range to another that are different from a value;
	\item  \emph{\textbf{remove\_if}}: moves all the elements of a range that obey a predicate to the end of the container and returns the iterator to the first moved element; 
	\item  \emph{\textbf{remove}}: moves all the elements of a range equal to a value to the end of the container and returns the iterator to the first moved element;
	\item  \emph{\textbf{replace\_copy\_if}}: copies all the elements of a range to another range replacing the elements that obey a predicate by a specified value;
	\item  \emph{\textbf{replace\_copy}}: copies all the elements of a range to another range replacing the elements equal to a given value by a specified value;
	\item  \emph{\textbf{replace\_if}}: replaces all the elements that obey a predicate by a specified value;
	\item  \emph{\textbf{replace}}: replaces all the elements of a range equal to a given value by a specified value;
	\item  \emph{\textbf{reverse\_copy}}: copies a range of size n, from end to beginning, to another range of size n;
	\item  \emph{\textbf{rotate\_copy}}: copies a left rotation of a range of elements to another range;
	\item  \emph{\textbf{rotate}}: performs a left rotation of a range of elements;
	\item  \emph{\textbf{sample}}: given a random number generator like {\textbf{mt19937}}, selects n elements of a given range and copies them to an output iterator;
	\item  \emph{\textbf{shuffle}}: given a random number generator like {\textbf{mt19937}}, shuffles the elements of a given range;
	\item  \emph{\textbf{stable\_partition}}: performs the same actions of  \emph{\textbf{partition}}, but preserves the relative order of elements. 
	\item  \emph{\textbf{swap\_ranges}}: swaps every element of a n-distance range with another n-distance range;
	\item  \emph{\textbf{swap}}: moves the values of two variables to one another;
	\item  \emph{\textbf{ranges::unique}}: removes duplicated values and returns a pair of [begin,end] iterators of the excess tail;
	

\end{itemize}

\subsubsection*{\textsc{Execution Policies} @$<$execution$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
  \item  {\textbf{execution::par}}: the algorithm will run parallelized in multiple threads;
\item  {\textbf{execution::par\_unseq}}: the algorithm may run with vectorization (single instruction in multiple data) in multiple threads; 
  \item  {\textbf{execution::seq}}: the algorithm will run sequentially in a single thread;
\item  {\textbf{execution::unseq}}: the algorithm may run with vectorization (single instruction in multiple data) in a single thread.

\end{itemize}


\subsubsection*{\textsc{Main Non-modifying Functions} @$<$algorithm$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{adjacent\_find}}: finds the first occurrence of two consecutive equal elements;
	\item  \emph{\textbf{binary\_search}}: finds the first element of a sorted range equal to a value;  
	\item  \emph{\textbf{clamp}}: given an upper and lower boundary and a value n, returns n if it is inside the range; otherwise returns the nearest boundary;  
	\item  \emph{\textbf{count\_if}}: returns the frequency of a value in a range that obey a predicate;
	\item  \emph{\textbf{count}}: returns the frequency of a value in a range; 
	\item  \emph{\textbf{equal}}: compares two ranges element by element, obeying or not a predicate;  
	\item  \emph{\textbf{find\_first\_if}}: finds the first element of a range that also belongs to another range;  
	\item  \emph{\textbf{find\_if}}: finds the first element of a range that obey a predicate;
	\item  \emph{\textbf{find}}: finds the first element of a range equal to a value; 
	\item  \emph{\textbf{for\_each}}: applies a callable object to the elements of a range;
	\item  \emph{\textbf{max}}: returns the biggest value between two values or in a initializer list;  
	\item  \emph{\textbf{ranges::max\_element}}: returns the address of the biggest value in a container;  
	\item  \emph{\textbf{min}}: returns the smallest value between two values or in a initializer list;  
	\item  \emph{\textbf{ranges::min\_element}}: returns the address of the smallest value in a container;  
	\item  \emph{\textbf{ranges::minmax\_element}}: returns a pair of address of the smallest and biggest values in a container;  	
	\item  \emph{\textbf{search}}: finds the first range of elements equal to a specified range of elements;  
	\item  \emph{\textbf{transform}}: transforms each element of one or more ranges according to a predicate (one or more parameters) and populate another range with the results;  
\end{itemize}


}

\softseparator

{\color{Blue}
\subsection*{\href{https://en.cppreference.com/w/cpp/thread}{\underline{CONCURRENCY}}}	
\noindent
Collection of libraries that provide functionalities and structures for concurrent programming.

\subsubsection*{\textsc{Theory} - \emph{Basics}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{atomic type}: a type whose operations are immune to data races;
	\item  \emph{condition variable}: flag that notifies a state to multiple threads;  
	\item  \emph{data race}: simultaneous thread accesses to shared resources;
	\item  \emph{deadlock}: threads A and B respectively lock resources 1 and 2 and respectively wait for 2 and 1 to be released;
	\item  \emph{mutex}: flag that notifies multiple threads whether to stop or resume execution; 
	\item  \emph{process}: a computer program in execution;
	\item  \emph{thread}: smallest unit of execution within a process;
\end{itemize}

\subsubsection*{\textsc{Atomic Types}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{atomic}} @ $<$atomic$>$: atomic class template for boolean, integral and floating point types;
	\item  \emph{\textbf{atomic\_ref}} @ $<$atomic$>$: enables atomic operations on non-atomic objects: 
\end{itemize}

\subsubsection*{\textsc{Threads}  @$<$thread$>$}
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  {\textbf{jthread}}: class providing thread functionalities that automatically joins on destruction;
	\item  \emph{\textbf{this\_thread::get\_id}}: returns the id of the current thread;
	\item  \emph{\textbf{this\_thread::sleep\_for}}: stops the execution of the current thread for at least a specified duration;
	\item  {\textbf{thread}}: class providing thread functionalities;
  \item  thread::\emph{\textbf{detach}}: parent thread keeps running and doesn't call terminate on child thread;
  \item  thread::\emph{\textbf{join}}: parent thread stops running to wait for child thread to end;
	\item  thread::\emph{\textbf{hardware\_concurrency}}: returns the maximum number of system's concurrent threads;
\end{itemize}

\subsubsection*{\textsc{Tasks}  @$<$future$>}
\noindent
Provides functionalities for obtaining values asynchronously (e.g. returning a value from a child thread to a parent thread).
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{async}}: executes a specified function asynchronously and returns a \textbf{future};
	\item  {\textbf{future}}: class that waits and returns the value stored asynchronously by its correspondent \textbf{promise};
	\item  {\textbf{promise}}: class that provides a way to store a value asynchronously;
	\item  {\textbf{shared\_future}}: a future whose copies are related to the same \textbf{promise} and then can be dispatched to different threads;
\end{itemize}

\subsubsection*{\textsc{Mutual Exclusions}}
\noindent
Provides functionalities to avoid multiple threads to simultaneously access shared resources (data races).
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
\item  {\textbf{adopt\_lock}} @ $<$mutex$>$: parameter for {\textbf{shared\_lock}} or {\textbf{unique\_lock}} to specify that the lock on the mutex was already performed.
	\item  {{\textbf{condition\_variable\_any}}} @ $<$condition\_variable$>$: provides a condition variable associated with any lock type.
	\item  {{\textbf{condition\_variable}}} @ $<$condition\_variable$>$: provides a condition variable associated with a {\textbf{unique\_lock}}.
\item  {\textbf{defer\_lock}} @ $<$mutex$>$: parameter for {\textbf{shared\_lock}} or {\textbf{unique\_lock}} to specify not to lock the mutex at the construction.
	\item  \textbf{lock\_guard} @ $<$mutex$>$: locks a \textbf{mutex} from the point where it is defined and automatically unlocks it on scope exit or exception throw.
	\item  {{\textbf{unique\_lock}}} @ $<$mutex$>$: locks a \textbf{mutex} from the point where it is defined and automatically unlocks it on scope exit or exception throw. It also allows manual unlocking and locking at specific points.
	\item  {\textbf{mutex}} @ $<$mutex$>$: provides simple mutual exclusion functionalities;
	 \item  mutex::\emph{\textbf{lock}} @ $<$thread$>$: tries to lock the mutex until it succeeds;
	 \item  mutex::\emph{\textbf{try\_lock}} @ $<$thread$>$: tries to lock the mutex and returns true or false;
\item  {\textbf{scope\_lock}} @ $<$mutex$>$: it is the \textbf{lock\_guard} version for simultaneous locking of more than one mutex.
	\item  {\textbf{shared\_lock}} @ $<$shared\_mutex$>$: locks a \textbf{shared\_mutex} from the point where it is defined and automatically unlocks it on scope exit or exception throw. It also allows manual unlocking and locking at specific points..
	\item  \textbf{shared\_mutex} @ $<$shared\_mutex$>$: a type of mutex in which while it is unique-locked, it cannot be unique or shared-locked; while shared-locked, it can be shared-locked but not unique-locked.
\end{itemize}

\subsubsection*{\textsc{Supported Task Functions}} 
\noindent
Function objects (functors), lambdas, variadic templates and member functions; 

}

\softseparator

{\color{Blue}
\subsection*{\href{https://en.cppreference.com/w/cpp/memory}{\underline{SMART POINTERS}}}	
\noindent
Wrappers around raw pointers with automatic heap memory deallocation allowing different types of ownership.

\subsubsection*{\textsc{Categories}  @$<$memory$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  {\textbf{shared\_ptr}}: smart pointer in which copy is enabled. The number of copies is registered;
	\item  {\textbf{unique\_ptr}}: smart pointer in which copy is disabled;
	\item  {\textbf{weak\_ptr}}: smart pointer in which copy is enabled. The number of copies is not registered. Dangling pointers can be checked.
\end{itemize}

\subsubsection*{\textsc{Helper Functions} @$<$memory$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{make\_shared}}: creates a \textbf{shared\_ptr} from a specified type and object;
	\item  \emph{\textbf{make\_unique}}: creates a \textbf{unique\_ptr} from a specified type and object;
\end{itemize}

\subsubsection*{\textsc{Function Parameters} @$<$memory$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  \emph{\textbf{f(unique\_ptr<T> ptr)}}: caller must pass the parameter by moving it (ownership transference);
	\item  \emph{\textbf{f(unique\_ptr<T> \&ptr)}}: function can change pointer and object states;
	\item  \emph{\textbf{f(shared\_ptr<T> ptr)}}: another \textbf{shared\_ptr} to the object will be created;
	\item  \emph{\textbf{f(shared\_ptr<T> \&ptr)}}: function can change pointer and object states;
\end{itemize}

}

\softseparator

{\color{Blue}
\subsection*{\href{https://en.cppreference.com/w/cpp/string}{\underline{STRINGS}}}	
\noindent
Collection of libraries that provide functionalities and structures for handling strings.

\subsubsection*{\textsc{Basic Strings}  @$<$string$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item  {\textbf{basic\_string}}: sequence of char type objects stored contiguously in memory;
	\item  {\textbf{basic\_string\_view}}: wrapper around a constant reference to \textbf{basic\_string};
\end{itemize}

\subsubsection*{\textsc{Aliases}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
	\item \textbf{string} $=$ \textbf{basic\_string}<char>;
	\item \textbf{string\_view} $=$ \textbf{basic\_string\_view}<char>;
	\item \textbf{wstring} $=$ \textbf{basic\_string}<wchar\_t>;
	\item \textbf{wstring\_view} $=$ \textbf{basic\_string\_view}<wchar\_t>;
\end{itemize}

\subsubsection*{\textsc{String functions} @$<$string$>$}
\begin{itemize}[leftmargin=*,topsep=0.25pt]
\setlength\itemsep{-1.8pt}

\item \emph{\textbf{append}}: appends characters or another string;
\item \emph{\textbf{at}}: accesses a character by index and  performs bounds checking;
\item \emph{\textbf{clear}}: removes all characters from the string;
\item \emph{\textbf{compare}}: compares strings lexicographically;
\item \emph{\textbf{contains}}: checks whether a string contains a given substring or character;
\item \emph{\textbf{data / c\_str}}: returns a pointer to the underlying character array;
\item \emph{\textbf{empty}}: checks whether the string has no characters;
\item \emph{\textbf{erase}}: removes characters from a given position or range;
\item \emph{\textbf{find / rfind}}: searches for a substring or character from the beginning or from the end;
\item \emph{\textbf{find\_first\_not\_of / find\_last\_not\_of}}: searches for a character not belonging to a given set;
\item \emph{\textbf{find\_first\_of / find\_last\_of}}: searches for any character from a given set;
\item \emph{\textbf{front / back}}: accesses the first or last character;
\item \emph{\textbf{insert}}: inserts characters or another string at a given position;
\item \emph{\textbf{push\_back / pop\_back}}: appends or removes one character at the end;
\item \emph{\textbf{replace}}: replaces part of the string with another string;
\item \emph{\textbf{reserve / capacity / shrink\_to\_fit}}: manages the internal storage capacity;
\item \emph{\textbf{resize}}: changes the string size, possibly adding characters;
\item \emph{\textbf{size / length}}: returns the number of characters in the string;
\item \emph{\textbf{starts\_with / ends\_with}}: checks whether a string has a given prefix or suffix;
\item \emph{\textbf{std::erase / std::erase\_if}}: convenient erase for \textbf{string} (\emph{no erase-remove boilerplate}). For conditional erase, keeps the elements that obeys the predicate;
\item \emph{\textbf{substr}}: creates a substring from a given position and length;
\end{itemize}



\subsubsection*{\textsc{Character Manipulation}  @$<$cctype$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
\item  \emph{\textbf{isalnum}}: checks if a character is alphanumeric;
\item  \emph{\textbf{isalpha}}: checks if a character is alphabetic;
\item  \emph{\textbf{isblank}}: checks if a character is a blank character;
\item  \emph{\textbf{iscntrl}}: checks if a character is a control character;
\item  \emph{\textbf{isdigit}}: checks if a character is a digit;
\item  \emph{\textbf{isgraph}}: checks if a character is a graphical character;
\item  \emph{\textbf{islower}}: checks if a character is lowercase;
\item  \emph{\textbf{isprint}}: checks if a character is a printing character;
\item  \emph{\textbf{ispunct}}: checks if a character is a punctuation character;
\item  \emph{\textbf{isspace}}: checks if a character is a space character;
\item  \emph{\textbf{isupper}}: checks if a character is an uppercase character;
\item  \emph{\textbf{isxdigit}}: checks if a character is a hexadecimal character;
\item  \emph{\textbf{tolower}}: converts a character to lowercase;
\item  \emph{\textbf{toupper}}: converts a character to uppercase;
\end{itemize}

\subsubsection*{\textsc{Conversions}  @$<$string$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
\item  \emph{\textbf{stod}}: converts a string to a double;
\item  \emph{\textbf{stof}}: converts a string to a float;
\item  \emph{\textbf{stoi}}: converts a string to a signed integer;
\item  \emph{\textbf{stold}}: converts a string to a long double;
\item  \emph{\textbf{stoll}}: converts a string to a signed long long;
\item  \emph{\textbf{stol}}: converts a string to a signed long;
\item  \emph{\textbf{stoull}}: converts a string to an unsigned long long;
\item  \emph{\textbf{stoul}}: converts a string to an unsigned long;
\item  \emph{\textbf{to\_string}}: converts an integral or floating-point value to string;
\item  \emph{\textbf{to\_wstring}}: converts an integral or floating-point value to wstring;
\end{itemize}

\softseparator

{\color{Blue}
\subsection*{\href{https://en.cppreference.com/w/cpp/utility}{\underline{UTILITIES}}}	
\noindent
Miscellaneous functionalities and structures.

\subsubsection*{\textsc{Date and Time} @$<$chrono$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
\item  {\textbf{chrono::system\_clock}}: represents the system's real time wall clock; 
\item  {\textbf{chrono::steady\_clock}}: represents the system's clock since last boot; 
\item  {\textbf{chrono::time\_point}}: represents a point in time; 
\item  {\textbf{chrono::duration}}: time duration; 
\item  {\textbf{chrono::hh\_mm\_ss}}: splits a \textbf{chrono::duration} in hours, minutes and seconds since midnight;
\item  {\textbf{time\_t}} @ $<$ctime$>$: time type that supports arithmetic operations;
\item  \emph{\textbf{to\_time\_t}} @ $<$ctime$>$: converts a \textbf{chrono::time\_point} to a {\textbf{time\_t}} type;
\item  \emph{\textbf{from\_time\_t}} @ $<$ctime$>$: converts a {\textbf{time\_t}} to a \textbf{chrono::time\_point};
\item  \textbf{tm} @ $<$ctime$>$: struct constituted by date and time elements;
\item  {\textbf{chrono::day}}: represents a day of the month;
\item  {\textbf{chrono::month}}: represents a month of the year;
\item  {\textbf{chrono::year}}: represents a year in the Gregorian calendar;
\item  {\textbf{chrono::weekday}}: represents a day of the week;
\item  {\textbf{chrono::month\_day}}: represents a day of the month;
\item  {\textbf{chrono::year\_month}}: represents a day of the month;
\item  {\textbf{chrono::year\_month\_day}}: represents a day of the month;
\end{itemize}

\subsubsection*{\textsc{Strucutures}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
\item  \emph{{\textbf{get<i>}}} @ $<$tuple$>$: returns the i-th element of the tuple;  
\item  \emph{\textbf{make\_pair}} @ $<$utility$>$: creates a \textbf{pair} from two objects, deducing their types; 
\item  \emph{\textbf{make\_tuple}} @ $<$tuple$>$: creates a \textbf{tuple} from various objects, deducing their types; 
\item  {\textbf{optional}} @ $<$optional$>$: declares a variable that may or may not have a value. It is useful for certain functions returning values;
\item  \emph{\textbf{tie}} @ $<$tuple$>$: assings each tuple value to especified predefined variables; 
\item  {\textbf{pair}} @ $<$utility$>$: relates two heterogeneous types in a single unit; 
\item  {\textbf{tuple}} @ $<$tuple$>$: relates many heterogeneous types in a single unit;

\end{itemize}

\subsubsection*{\textsc{Random and Other Numbers}  @$<$random$>$} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
\item  \emph{\textbf{bernoulli\_distribution}}: a Bernoulli distribution of generated numbers within a specified range; 
\item  \textbf{bitset} @ $<$bitset$>$: class that represents a binary number on which  it is possible to perform bitwise operations;
\item  \textbf{complex} @ $<$complex$>$: class that represents a complex number on which  it is possible to perform a variety of artihmetic operations;
\item  \emph{\textbf{exponential\_distribution}}: a exponential distribution of generated numbers within a specified range; 
\item  \emph{\textbf{mt19937\_64}}: generator of random numbers based on 64bit Marsenne Twister algorithm;  
\item  \emph{\textbf{mt19937}}: generator of random numbers based on 32bit Marsenne Twister algorithm;  
\item  \emph{\textbf{normal\_distribution}}: a normal distribution of generated numbers within a specified range; 
\item  \emph{\textbf{poisson\_distribution}}: a Poisson distribution of generated numbers within a specified range; 
\item  \emph{\textbf{random\_device}}: a random seed for the generator; 
\item  \emph{\textbf{uniform\_int\_distribution}}: a uniform int distribution of generated numbers within a specified range; 
\item  \emph{\textbf{uniform\_real\_distribution}}: a uniform real distribution of generated numbers within a specified range;

\end{itemize}

\subsubsection*{\textsc{Miscellaneous}} 
\begin{itemize}[leftmargin=*,topsep=0.25pt]
  \setlength\itemsep{-1.8pt}
\item  \emph{\textbf{assert}} @ $<$cassert$>$: run time checking of boolean expression; 
\item  \emph{\textbf{cref}}: the const version of \emph{\textbf{ref}};
\item  \emph{\textbf{ref}}: returns an object of type \textbf{reference\_wrapper} from an lvalue. Used when a function and its arguments are passed as parameters in another function;
\item  \emph{\textbf{static\_assert}}: compile time checking of boolean expression;
\item  {\textbf{thread\_local}}: each thread accessing a variable declared with this type specifier will have its  own copy of the variable; 
\end{itemize}

}

}

 

\end{multicols*}

\end{document}
